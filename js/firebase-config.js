/* ============================================================
   Firebase 설정
   ------------------------------------------------------------
   폼 제출 저장 · 관리자 로그인 · 링크/공지 실시간 관리에 사용합니다.

   ▸ 비워두면 사이트는 '로컬 모드'로 동작합니다.
     - 공개 페이지: 정상 동작 (공지는 data/notices.json, 링크는 js/config.js)
     - 폼: '준비 중' 으로 표시되고 제출은 되지 않습니다
     - 관리자 페이지: 로그인 없이 열리며, 입력한 내용은
       그 브라우저에만 저장됩니다 (기능을 미리 보는 용도)

   ▸ 설정 방법은 README 의 'Firebase 연결하기' 를 보세요.
     Firebase 콘솔 → 프로젝트 설정 → 내 앱 → 웹 앱의 firebaseConfig 를
     그대로 붙여넣으면 됩니다.

   ※ 이 값들은 비밀이 아닙니다. 웹 앱에 공개되는 것이 정상이며,
     실제 보안은 firestore.rules 의 보안 규칙이 담당합니다.
     반드시 README 대로 규칙을 배포하세요.
   ============================================================ */

window.FIREBASE_CONFIG = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: ''
};

/* Firebase JS SDK 버전. 새 버전으로 올리려면 이 값만 바꾸세요. */
window.FIREBASE_SDK_VERSION = '10.14.1';
